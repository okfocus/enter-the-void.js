h1. oktype by OKFocus

p. OKType is a highly customizable jQuery plugin that allows for animation of typogragphy styles on the web.

h3. Requirements

p. OKType depends on jQuery (> v1.4).

h3. Usage

p. OKType should be bound to elements that contains type (headers, paragraphs, spans, etc).

p. Apply OKType to an element on your webpage like so:

bc. <script>
  $(document).ready(function(){
      $('h1').oktype();  
  });
</script>

p. When you hover on an @<h1>@, you'll now see the OKType changing the type style. To make a more interesting animation, add an option:

bc. <script>
  $(document).ready(function(){
      $('h1').oktype({ randomColor: true });
  });
</script>

h3. Options

p. The following options are available to you:

|_.option|_.description|_.default|
|weight|alternates the font weight by toggling or selecting at random (a string - 'toggle' or 'random')|null|
|minSize|the minimum desired size for the type (an int)|null|
|maxSize|the maximum desired size for the type (an int)|null|
|randomColor|randomizes type color (a boolean)sets the size of the shadow in pixels|false|
|randomDecoration|Random from array of text-decoration rules (a boolean)|false|
|interval|the speed of our animation in milliseconds (an int)|100|
|toggleCase|randomizes capitalization and lowercase of type (a boolean)|false|
|minSpace|the maximum desired letter-spacing for type (an int)|null|
|maxSpace|the minimum desired letter-spacing for type (an int)|null|
|restore|restore the element to its prior state when not hovering (a boolean)|true|
|hover|execute animation on hover, otherwise always animate (a boolean)|true|

h3. Run Tests

p. OKFocus tests JavaScript with Jasmine. After cloning this repo simply run:

bc. $ bundle install
$ rake jasmine
